// ADDIM 11: /competition/:id — yarış otağı + Socket.io + age-adaptive arena
export default function CompetitionRoom() {
  return (
    <div className="min-h-screen bg-[#0D0D0D] flex items-center justify-center">
      <p className="text-white text-xl font-semibold">Yarış Otağı — tezliklə...</p>
    </div>
  )
}
